import { useEffect } from 'react';

function App() {
  useEffect(() => {
    // This project uses standalone HTML files (index.html, admin.html, pdv.html)
    // The Vite build inlines index.html as the entry point
  }, []);

  return null;
}

export default App;
